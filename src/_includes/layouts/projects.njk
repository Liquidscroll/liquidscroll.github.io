---
layout: layouts/base.njk
---

<article class="projects-overview">
  <h1>Projects</h1>
  <p>
    Welcome to my projects page! Here you’ll find a collection of my latest
    coding and robotics creations, from simple scripts to complex applications.
    These projects showcase my skills and passions as a computer science
    student, and I’m excited to share them with you. Take a look around and let
    me know what you think!
  </p>
  <p>
    Below is a selection of my personal and academic projects. Each entry
    includes a short summary and the key technologies involved.
  </p>
  <table class="projects-table">
    <thead>
      <tr>
        <th>Project</th>
        <th>Summary</th>
        <th>Tech</th>
        <th>Link</th>
      </tr>
    </thead>
    <tbody>
      {% for proj in projects %}
      <tr>
        <td>{{ proj.title }}</td>
        <td>{{proj.summary}}</td>
        <td>
          {%- if proj.tech -%} {{ proj.tech | join(', ') }} {%- else -%} — {%-
          endif -%}
        </td>
        <td>
          {%- if proj.link -%}
          <a href="{{ proj.link }}">{{proj.link}}</a>
          {%- else -%} — {%- endif -%}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</article>
